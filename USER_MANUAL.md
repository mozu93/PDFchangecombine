# PDF変換・結合ツール ユーザーマニュアル

## 📖 目次

1. [はじめに](#はじめに)
2. [インストール](#インストール)
3. [基本的な使い方](#基本的な使い方)
4. [PDF結合機能](#pdf結合機能)
5. [資料の差し替え・構成変更機能](#資料の差し替え構成変更機能)
6. [資料番号挿入機能](#資料番号挿入機能)
7. [ページ番号挿入機能](#ページ番号挿入機能)
8. [ファイル変換機能](#ファイル変換機能)
9. [トラブルシューティング](#トラブルシューティング)
10. [よくある質問](#よくある質問)
11. [バージョン履歴](#バージョン履歴)

---

## はじめに

PDF変換・結合ツールは、以下の機能を提供する便利なアプリケーションです：

- **ファイル変換**: Word・Excel・PowerPoint・画像ファイルをPDFに変換
- **資料番号挿入**: PDFに「資料1」「資料2」などの番号を自動挿入
- **PDF結合**: 複数のPDFファイルを1つにまとめる
- **資料の差し替え・構成変更**: 結合済みPDFの中の1資料だけを差し替え・追加・削除
- **ページ番号挿入**: PDFのフッター中央にページ番号を挿入

### 🎯 こんな方におすすめ

- 複数の資料をまとめてPDF化したい
- 会議資料に番号を付けたい
- 様々な形式のファイルを統一したい
- 結合済みの資料集から1つの資料だけ差し替えたい
- 簡単操作でプロ品質の仕上がりを求める

---

## インストール

### システム要件

- **OS**: Windows 10/11 (64bit)
- **メモリ**: 4GB以上推奨
- **ディスク**: 500MB以上の空き容量

### インストール手順

1. **インストーラーをダウンロード**
   - [Releases](https://github.com/mozu93/PDFchangecombine/releases/latest) から `PDFConverter-setup.exe` をダウンロード

2. **インストーラーを実行**
   - ダウンロードしたファイルをダブルクリックし、画面の指示に従って進める
   - セキュリティ警告が表示された場合は「詳細情報」→「実行」を選択

3. **起動確認**
   - インストール完了後、スタートメニューまたはデスクトップアイコンからアプリケーションを起動し、正常に立ち上がることを確認

> **注意**: 初回起動時は、Windowsセキュリティの警告が表示される場合があります。「詳細情報」→「実行」を選択してください。
> Word・Excel・PowerPointの変換にはMicrosoft Office（デスクトップ版）が別途必要です。Officeが無くても、PDF結合・画像変換・資料NO/ページ番号挿入は利用できます。

---

## 基本的な使い方

### アプリケーション起動

1. `PDF変換結合ツール.exe` をダブルクリック
2. メイン画面が表示されます

### メイン画面の構成

アプリケーションは4つのタブで構成されており、画面左のサイドバーから切り替えます：

| タブ | 機能 | 用途 |
|------|------|------|
| **変換** | ファイル形式変換 | Word/Excel/PowerPoint/画像 → PDF |
| **資料NO** | 資料番号挿入 | PDF に「資料1」等の番号を追加 |
| **結合** | PDF結合・資料の差し替え | 複数PDF → 1つのPDF、結合済みPDFの構成変更 |
| **ページ番号** | ページ番号挿入 | PDFのフッターにページ番号を追加 |

各タブの実行ボタンの直上には「保存先」の行があり、どこに保存されるか（フォルダ名＋場所）を実行前に確認できます。処理完了後に表示される帯（バナー）にも保存先が表示されます。

### ファイル追加方法

以下の方法でファイルを追加できます：

1. **ドラッグ&ドロップ**: ファイルやフォルダをウィンドウにドラッグ
2. **ファイル選択ボタン**: 各タブのボタンからダイアログでファイルを選択
3. **フォルダ指定**: フォルダをドラッグすると中のファイルを自動検索して一括追加

複数ファイルの同時追加に対応しています。**元ファイルは変更されず、そのままの場所に残ります。**

---

## PDF結合機能

複数のPDFファイルを1つにまとめる機能です。

### 📋 使用手順

#### 1. 「結合」タブを選択

#### 2. PDFファイルを追加

- **ファイル選択**: 「PDF選択」ボタンをクリックしてPDFファイルを複数選択
- **ドラッグ&ドロップ**: エクスプローラーでPDFファイルを選択し、アプリ画面にドラッグ

#### 3. ファイル順序の調整

- **ドラッグ**: リスト内のファイルをドラッグして並び替え
- **↑ / ↓ ボタン**: 選択したファイルを1つ上/下へ移動
- **Ａ↓ ボタン**: ファイル名の昇順（自然順）に並び替え
- **選択削除 / クリア**: 選択したファイルだけ、または全ファイルをリストから削除

> **ポイント**: ファイルの順序が結合後のページ順序になります

#### 4. オプション設定（「▼ 詳細設定」を開く）

**奇数ページ末尾に白紙ページを挿入**
- 両面印刷時に次のPDFが必ず奇数ページから始まるよう、末尾ページ数が奇数のPDFに白紙ページを追加します

**ページ番号を追加**
- 結合後の各ページのフッター中央にページ番号を印刷します
- 開始ページ: 何ページ目からページ番号を印刷するか
- 開始番号: ページ番号の開始値
- 左綴じ対応: A3縦・横長ページのページ番号位置を左綴じ製本向けに変更

#### 5. 結合実行

1. 「📋 PDF結合実行」ボタンをクリック
2. 出力先フォルダに **「【結合】（先頭ファイル名）.pdf」** という名前で自動保存されます（保存先を指定するダイアログは表示されません）
3. 同名ファイルが既に存在する場合は、上書きするか確認するダイアログが表示されます

### 💡 活用例

**会議資料の統合**
```
議事次第.pdf + 資料A.pdf + 資料B.pdf + 参考資料.pdf
→ 【結合】議事次第.pdf
```

**レポートの章統合**
```
表紙.pdf + 第1章.pdf + 第2章.pdf + 第3章.pdf
→ 【結合】表紙.pdf（ページ番号付き）
```

---

## 資料の差し替え・構成変更機能

本アプリで結合したPDF（資料番号の構成情報を内部に持つもの）を選び、資料単位での差し替え・追加・削除・一括リナンバリングができる機能です。

### 📋 使用手順

#### 1. 「結合」タブの「資料を差し替え...」ボタンをクリック

対象の結合済みPDFを選択します（ドラッグ&ドロップでの読み込みにも対応）。

#### 2. 変更したい内容を指定する

資料ごとに以下のボタンで操作します。**この時点ではまだファイルは作られません。**

- **差し替え...**: その資料を別のPDFファイルに差し替える
- **＋この後に追加**: その資料の直後に新しい資料を追加する
- **削除**: その資料を削除する

指定した変更は一覧に「差し替え予定」「追加予定」「削除予定」として積み上がります。「取り消す」ボタンで個別に、「変更をすべて取り消す」ボタンでまとめて撤回できます。

#### 3. 変更を実行する

すべての変更を指定したら「変更を実行」ボタンをクリックします。内容の確認後、**指定した変更がまとめて1回だけ適用され、出力ファイルが1つだけ作成されます**（操作のたびに中間ファイルが増えることはありません）。

#### 4. 一括リナンバリング（任意）

資料の削除・追加により資料番号が欠番（資料1、資料3…）や枝番（資料1.5）になった場合、「🔢 一括リナンバリング...」で連番に振り直せます。積んだ変更予定が残っている間は実行できません。

> **注意**: 元データが確認できない資料（資料NOタブを経由せず直接結合された資料など）は、一括リナンバリングの対象外としてスキップされます。

いずれの操作も、元の結合済みPDFは変更されず、新しいファイルとして出力されます。

---

## 資料番号挿入機能

PDFのヘッダー右上に「資料1」「参考2」などの番号を自動で挿入する機能です。

### 📋 使用手順

#### 1. 「資料NO」タブを選択

#### 2. PDFファイルを追加

結合機能と同様に、ドラッグ&ドロップまたはボタンでファイルを追加します。

#### 3. 文字を選択

| 選択肢 | 説明 |
|--------|------|
| **資料** | 「資料1」「資料2」... |
| **参考** | 「参考1」「参考2」... |
| **その他** | 入力欄に任意の文字を入力（例: 別紙、付録） |

#### 4. 番号方式を選択

| モード | 説明 | 使用例 |
|--------|------|--------|
| **連番** | 指定番号から順番に番号付け | 「資料1」「資料2」「資料3」 |
| **ハイフン連番** | プレフィックス付きの連番 | 「資料1-1」「資料1-2」「資料1-3」 |
| **固定番号** | 全ファイルに同一番号を挿入 | 「資料5」を全ファイルに |
| **番号なし** | 番号を付けず文字のみ挿入 | 「資料」のみ |

#### 5. その他のオプション（詳細設定）

- **フォントサイズ**: 20 / 18 / 16 / 14 / 12 pt から選択
- **枠内の背景**: 透明 または 白色
- **全ページ/表紙のみ**: 全ページに挿入するか、表紙（1ページ目）のみにするか
- **ファイル名への追加**: オンにするとファイル名の先頭に「【資料１】」が付加されます
- **A3縦・A4横ページの左綴じ対応**: A3縦やA4横のPDFを、左綴じ資料に挟んだ際に読める向き（右下）へ自動検出して挿入します

#### 6. 資料番号挿入実行

1. 「資料NO挿入実行」ボタンをクリック
2. 確認ダイアログで内容を確認し「はい」をクリック
3. 各ファイルの処理結果（成功✓/失敗✕）がリストに表示されます

### 📍 資料番号の配置

- **位置**: 各ページの右上角
- **デザイン**: 黒文字、四角い枠付き（枠内は透明または白色を選択可能）
- **フォント**: 日本語対応の見やすいフォント
- **回転対応**: ページが回転していても正しい位置・向きに配置

### 💡 活用例

**会議資料の番号付け**
```
提案書.pdf + 計画書.pdf + 予算書.pdf
↓ ハイフン連番モード「1」
資料1-1、資料1-2、資料1-3
```

**研修資料の整理**
```
基礎編.pdf + 応用編.pdf + 実践編.pdf
↓ 連番モード
資料1、資料2、資料3
```

---

## ページ番号挿入機能

PDFのフッター中央にページ番号を挿入して保存する機能です。PDF結合時に一緒に付けることもできますが、既に結合済みのPDFに後から付けたい場合はこちらを使います。

### 📋 使用手順

1. 「ページ番号」タブを選択
2. PDFファイルをドラッグ&ドロップまたはボタンで追加
3. 開始ページ・開始番号を入力
4. 左綴じ対応が必要な場合はオプションをオンにする（A3横はページ左半分中央下、A4横・A3縦は左端中央にページ番号を挿入）
5. 「ページ番号挿入実行」ボタンをクリック

出力先フォルダに新規PDFが作成されます。元ファイルは変更されません。

> **ポイント**: 資料の差し替え機能で使う結合済みPDFにこの機能でページ番号を追加した場合も、内部の資料構成（資料NOごとの区切り）は保持されるため、後から資料を差し替えても他の資料やページ番号はそのまま残ります。

---

## ファイル変換機能

Word・Excel・PowerPoint・画像ファイルをPDFに変換する機能です。

### 📋 使用手順

#### 1. 「変換」タブを選択

#### 2. ファイルを追加

**対応形式**
- **Office文書**: .docx, .doc, .xlsx, .xls, .pptx, .ppt
- **画像ファイル**: .jpg, .jpeg, .png, .bmp, .gif, .tiff
- **PDF**: そのまま出力先にコピーされます

**Excelオプション**: オンにすると、複数シートあるExcelを各シートごとに個別のPDFとして出力します。オフの場合は印刷設定に従い1ファイルに出力します。

#### 3. 変換実行

1. 「PDF変換実行」ボタンをクリック
2. 変換処理が開始されます（処理中は「⏹ キャンセル」ボタンで、処理中のファイルが終わった時点で安全に中断できます）
3. 完了後、各ファイルの行に成功(✓)・失敗(✕)が表示されます。失敗したファイルだけ「失敗のみ再実行」ボタンでやり直せます

#### 4. 次のタブへ送る（任意）

処理完了時に表示される帯（バナー）から、変換したPDFをそのまま次の作業タブへ渡せます。

- **「→ 資料NO挿入タブへ送る」**: 資料NO挿入タブに切り替え、変換済みファイルが追加された状態になる
- **「→ 結合タブへ送る」**: 結合タブに切り替え、変換済みファイルが追加された状態になる

### 💡 活用例

**プレゼン資料の統合**
```
スライド.pptx + データ.xlsx + 参考画像.jpg
↓ 変換
スライド.pdf + データ.pdf + 参考画像.pdf
↓ 結合タブへ送る → 結合
プレゼン資料_完成版.pdf
```

---

## トラブルシューティング

### よくある問題と解決方法

#### 🚫 ファイルが追加できない

**症状**: ファイルをドラッグしても追加されない

**解決方法**:
1. ファイル形式を確認（対応形式か確認）
2. ファイルサイズを確認（100MB以下か確認）
3. ファイルが他のアプリで開かれていないか確認
4. ボタンから手動でファイル選択を試す

#### ⚠️ 変換に失敗する

**症状**: 「変換に失敗しました」のエラーメッセージ

**解決方法**:
1. **Officeファイルの場合**:
   - Microsoft Officeがインストールされているか確認
   - ファイルが破損していないか確認
   - 変換中に対象ファイルを開いたまま操作しない

2. **画像ファイルの場合**:
   - ファイルが破損していないか確認
   - 別の画像形式に変換してから再実行

3. **OneDrive・SharePoint上のファイルの場合**:
   - ファイルのロック競合を自動回避する処理が組み込まれていますが、それでも失敗する場合はローカルフォルダにコピーしてから変換してください

#### 🔒 権限エラー

**症状**: 「アクセス権限がありません」のエラー

**解決方法**:
1. 保存先フォルダの書き込み権限を確認
2. ファイルが読み取り専用になっていないか確認
3. 他のアプリでファイルが開かれていないか確認

#### 💾 保存に失敗する

**症状**: PDF保存時にエラーが発生

**解決方法**:
1. 保存先の空き容量を確認
2. 別の保存先を選択（各タブの出力先フォルダ変更から）
3. ファイル名に使用できない文字がないか確認
4. 同名ファイルが既に存在する場合は上書き確認ダイアログが表示されます。上書きしたくない場合は「いいえ」を選ぶと連番付きの別ファイルとして保存されます

### エラーメッセージ一覧

| エラーメッセージ | 原因 | 対処法 |
|------------------|------|--------|
| ファイルが見つかりません | ファイルパスが無効 | ファイルの存在を確認 |
| アクセス権限がありません | 読み書き権限なし | ファイル・フォルダの権限確認 |
| ファイルサイズが大きすぎます | 100MB超過 | ファイルサイズを削減 |
| 対応していない形式です | 非対応ファイル形式 | 対応形式に変換 |
| メモリが不足しています | RAM不足 | 他のアプリを終了 |

---

## よくある質問

### 📝 基本的な質問

**Q1: どのようなファイル形式に対応していますか？**

A: 以下の形式に対応しています：
- **入力**: PDF, Word(.docx/.doc), Excel(.xlsx/.xls), PowerPoint(.pptx/.ppt), 画像(.jpg/.png/.bmp/.gif/.tiff)
- **出力**: PDF
- Word・Excel・PowerPointの変換にはMicrosoft Office（デスクトップ版）のインストールが必要です

**Q2: ファイルサイズの制限はありますか？**

A: 1ファイルあたり100MBまでです。それ以上のファイルは事前に圧縮してください。

**Q3: 一度に何個のファイルを処理できますか？**

A: 制限はありませんが、大量のファイル（100個以上）を処理する場合は時間がかかることがあります。

### 🔧 機能に関する質問

**Q4: 資料番号の位置は変更できますか？**

A: 現在のバージョンでは右上角に固定されています。位置の変更はできません。

**Q5: ページ番号と資料番号を同時に追加できますか？**

A: はい。資料NO挿入タブで番号を挿入した後、結合タブの「ページ番号を追加」オプションを有効にして結合すれば、両方を一度に付けられます。既に結合済みのPDFに後からページ番号だけ追加したい場合は、別途「ページ番号」タブを使用してください。

**Q6: 変換した元ファイルはどうなりますか？**

A: 元ファイルは移動・変更されず、そのままの場所に残ります（バックアップフォルダへの退避は行われません）。

**Q7: 結合済みのPDFから1つの資料だけ差し替えたい場合は？**

A: 「結合」タブの「資料を差し替え...」ボタンから、差し替え・追加・削除ができます。詳しくは[資料の差し替え・構成変更機能](#資料の差し替え構成変更機能)を参照してください。

### 💻 技術的な質問

**Q8: インターネット接続は必要ですか？**

A: いいえ。すべての処理はローカルで実行されるため、インターネット接続は不要です（新バージョンの確認機能を使う場合を除く）。

**Q9: 個人情報は送信されますか？**

A: いいえ。ファイル内容やログ情報が外部に送信されることはありません。

**Q10: アンインストール方法は？**

A: Windowsの「設定」→「アプリ」（または「コントロールパネル」→「プログラムのアンインストール」）から「PDF変換・結合ツール」を選んでアンインストールしてください。スタートメニューのアンインストーラーからも実行できます。

### 🎨 品質に関する質問

**Q11: 変換後の品質は劣化しますか？**

A: 可能な限り元の品質を保持します。ただし、形式変換により若干の変化が生じる場合があります。

**Q12: 日本語フォントは正しく表示されますか？**

A: はい。日本語に最適化されたフォントを使用しており、文字化けは発生しません。

---

## サポート情報

### 📞 お困りの時は

1. **このマニュアルを確認**: トラブルシューティング参照
2. **アプリ内ヘルプを確認**: ヘッダーの「？」ボタンから使い方・バージョン情報を確認できます
3. **ログファイルを確認**: `%APPDATA%/PDF変換・結合ツール/logs/` フォルダ

### 📊 バージョン情報

- **現在のバージョン**: v1.22.0
- **最終更新**: 2026年8月
- **動作確認環境**: Windows 10/11

### 🔗 関連ドキュメント

- **開発者向け**: `PRODUCTION_DEPLOYMENT.md`
- **技術仕様**: `README.md`

---

## バージョン履歴

| バージョン | 内容 |
|------------|------|
| 1.22.0 | PDFのページ単位で編集できる「ページ編集」タブを追加。ページをサムネイルで一覧表示し、削除・並べ替え・抽出（選んだページだけを別ファイルに出力）・挿入（別PDFの全ページを指定位置へ挿入）に対応。Ctrl+Zで1手ずつ取り消し可能。元のファイルは変更されず、「ページ編集済」フォルダに保存 |
| 1.21.9 | 処理完了バナーのボタン・閉じるボタンが幅0になり操作不能になる不具合を修正。保存先行のツールチップが表示されない不具合を修正。保存先にドライブ直下を指定した際にフォルダ名が空欄になる不具合を修正。配布用実行ファイルがファイルサイズ上限（50MB）を含む開発時設定のまま配布されていた不具合を修正（正しくは100MB）。未使用の依存関係・設定・コードを整理し、LICENSE（MIT）を追加 |
| 1.21.8 | バージョンアップ時のリリースノートポップアップが配布用実行ファイルで表示されない不具合を修正（ビルド設定への同梱漏れ）。GitHubリリースページの「変更内容」に実際のリリースノートが表示されるようビルド手順を修正 |
| 1.21.7 | 「資料を差し替え」機能（PDF結合タブから、資料単位での差し替え・追加・削除・一括リナンバリングが可能）を追加。複数の変更をまとめて1回で実行できるように変更し、出力ファイルが都度増える問題を解消。差し替えダイアログにドラッグ&ドロップ対応を追加。結合済みPDFに後からページ番号を付けても資料構成が保持されるよう修正。変換・資料NO挿入・結合・ページ番号の全機能で、同名ファイルの上書き確認に対応。バージョンアップ時にリリースノートをポップアップ表示。ファイルリストの単一選択・ツールバー幅など細かいUI修正 |
| 1.21.6 | 資料NO挿入・PDF結合タブの詳細設定を折りたたみ式に変更し画面をすっきり表示。タブ表示をアイコン+短縮ラベルに変更（正式名称はホバーで表示）。ドラッグ&ドロップ時の挿入位置表示とドラッグ中のファイルの見た目を改善 |
| 1.21.5 | Excel変換でA4 PDFのページ寸法が約4/3倍になった場合、A4印刷時に資料NOが縮小されないよう文字・余白・枠・白背景を自動補正 |
| 1.21.4 | 資料NOの四角い枠内を白色にできるオプションを追加。透明／白色を選択でき、設定保存とプレビューに対応 |
| 1.21.3 | 出力先を記憶せず機能別既定フォルダ（PDF変換済・資料NO挿入済・PDF結合済・ページ番号挿入済）へ保存する仕様に変更。結合ファイル名を「【結合】先頭ファイル名.pdf」に変更。元ファイルの「変換元」フォルダ退避を廃止（元の場所に残置） |
| 1.21.2 | 資料NO挿入時、90度・180度回転ページで資料NOが誤った位置・向きに挿入される不具合を修正 |
| 1.21.1 | 資料NOフォントサイズに14pt・12ptを追加。①などの機種依存文字はBIZ UDPゴシック選択時のみ入力エラーとして拒否するよう修正 |
| 1.21.0 | 処理完了後、元ファイルを「変換元」フォルダへ自動移動する機能を全タブに追加 |
| 1.20.1 | ページ番号挿入時、90度回転ページで数字が上下逆さまに表示される不具合を修正 |
| 1.20.0 | 出力先統一・非破壊的出力への変更。全タブに出力先フォルダ指定UI追加。結合タブの保存先ダイアログ廃止（自動ファイル名）。元ファイルバックアップフォルダ廃止 |
| 1.19.0 | マニュアルを最新版に全面更新 |
| 1.16.1 | 資料NO挿入タブの設定順を変更（文字選択→番号設定→フォント） |
| 1.16.0 | 資料NO挿入・ページ番号挿入タブにフォント選択プルダウン追加 |
| 1.15.0 | OneDrive/SharePoint クラウドパス検出を強化（レジストリ読み取りによる組織名不問の自動検出） |
| 1.14.0 | Excel全シートPDF変換の空ページエラー修正 |
| 1.13.0 | 変換後・資料NO挿入後の自動タブ切替ダイアログを廃止 |
| 1.12.0 | 資料NO挿入に左綴じ対応・全ページ挿入・プレビュー機能を追加 |
| 1.11.1 | インストーラー形式での配布対応、アップデート通知機能追加 |
| 1.0.0 | 初回リリース |

---

*このマニュアルは予告なく更新される場合があります。最新版は GitHub リポジトリでご確認ください。*
