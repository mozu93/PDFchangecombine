# PDF変換・結合ツール v1.21.9

## 新機能・UI改善

- **タブメニューを左サイドバーに移動**: 横並びだったタブを画面左のサイドバー（幅130px固定）に変更し、アイコン＋正式名称（PDF変換／資料NO挿入／PDF結合／ページ番号挿入）を2行表示にして、どの機能かひと目で分かるようにしました。
- **保存先をフォルダ名主体で表示**: 各タブの実行ボタン直上に「保存先」の行を新設し、フォルダ名と場所を実行前に確認できるようにしました。処理完了時のバナーにも保存先を表示します。

## 不具合修正

- 上記の保存先表示を追加したことで、ウィンドウ幅が狭い環境では処理完了バナーの操作ボタン・閉じるボタン（✕）が幅0になり、押せなくなる不具合を修正しました。
- 保存先の項目にマウスを乗せてもツールチップ（フルパス表示）が出ない不具合を修正しました。
- 保存先にドライブの直下（例: `D:\`）を指定した場合に、フォルダ名の表示が空欄になる不具合を修正しました。
- 配布用実行ファイルで、ファイルサイズの上限が意図せず開発時の設定（50MB）のまま配布されていた不具合を修正しました。正しくは100MBです。

## その他

- ライセンス（MIT License）を追加しました。
- 未使用の依存関係・設定・コードを整理し、動作に影響しない範囲で保守性を改善しました。
