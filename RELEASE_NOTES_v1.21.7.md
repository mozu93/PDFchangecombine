# PDF変換・結合ツール v1.21.7

## 新機能

- **資料を差し替え...**: PDF結合タブから、本アプリで結合したPDFを選び、資料単位での「差し替え」「追加」「削除」「一括リナンバリング」ができるようになりました。
  - 変更は一覧に「差し替え予定」「追加予定」「削除予定」として積み上げられ、「変更を実行」をクリックした時点でまとめて1回だけ適用されます。操作のたびにファイルが増えていくことがなくなりました。
  - 対象PDFの選択欄はドラッグ&ドロップに対応しています。
- **同名ファイルの上書き確認**: 変換・資料NO挿入・結合・ページ番号挿入のいずれでも、出力先に同名ファイルが既にある場合は上書きするかどうかの確認ダイアログが表示されるようになりました。「いいえ」を選んだ場合のみ、従来通り連番付きの別ファイルになります。
- **更新内容のポップアップ表示**: アプリのバージョンが更新されると、次回起動時に「更新内容」ポップアップでこのリリースノートが自動表示されるようになりました。

## 不具合修正

- 結合済みPDFに対して「ページ番号」タブで後からページ番号を追加すると、内部の資料構成情報が1件に潰れてしまい、以後の資料差し替え・追加操作で他の資料やページ番号が失われることがある不具合を修正しました。
- 資料NOタブ→結合タブの自動連携を使わず、資料NO挿入済みのPDFを手動で結合タブに読み込んだ場合に、資料番号が「(番号なし)」として扱われてしまう問題を修正しました（資料番号情報をPDFファイル自体に埋め込むことで解消）。

## UI改善

- 資料差し替えダイアログの資料一覧で、ファイル名が長いと「差し替え」「削除」ボタンが画面外に見切れる問題を修正しました。
- ファイルリストで複数の項目をクリックすると、意図せず複数が選択状態（水色）のまま残る問題を修正し、常に最後にクリックした1件だけが選択されるようにしました。
- PDF結合タブのツールバー（「資料を差し替え...」ボタン追加後）が初期ウィンドウ幅に収まらず一部ボタンが見切れる問題を修正しました。
- ファイル名にホバーした際のフルパス表示ツールチップを削除しました。
- 資料差し替えダイアログの確認メッセージで、資料番号が未設定の資料が空欄「」と表示される代わりに「資料NOなし」と表示されるようにしました。
