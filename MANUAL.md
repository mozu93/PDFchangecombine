# PDF変換・結合ツール ユーザーマニュアル

バージョン 1.20.0

---

## 概要

PDF変換・結合ツールは、Office文書・画像ファイルのPDF変換、複数PDFの結合、資料番号挿入、ページ番号挿入をワンストップで行えるツールです。

**すべての処理において元ファイルは変更されません。** 出力は指定フォルダに新規ファイルとして作成されます。

### 対応ファイル形式

| 種類 | 拡張子 |
|------|--------|
| Word | .docx, .doc |
| Excel | .xlsx, .xls |
| PowerPoint | .pptx, .ppt |
| 画像 | .jpg, .jpeg, .png, .bmp, .gif, .tiff |
| PDF | .pdf |

---

## 共通操作

### ファイルの追加方法

- **ドラッグ&ドロップ**: ファイルやフォルダをウィンドウ上にドロップ。フォルダをドロップすると内部のファイルを自動検索します。
- **ボタン**: 「ファイル選択」「PDF選択」ボタンをクリックしてダイアログから選択。

### ファイルの並び替え

- リスト内のファイルをドラッグして順序を変更できます（資料NO挿入・PDF結合タブ）。
- ↑ / ↓ ボタンでも順序を変更できます。
- **Ａ↓** ボタンでファイル名の昇順に自動ソートできます。

### 出力先フォルダの指定（全タブ共通）

各タブに **「出力先: …」** の行があります。

| 操作 | 動作 |
|------|------|
| ファイルを最初にドロップ | ドロップしたファイルの親フォルダが自動設定される |
| **📂 変更** ボタン | フォルダ選択ダイアログで任意のフォルダに変更できる |

**同名ファイルが既に存在する場合**、自動的に `ファイル名 (2).pdf` → `ファイル名 (3).pdf` のように連番が付与されます。元ファイルは上書きされません。

---

## PDF変換タブ

Office文書・画像ファイルをPDFに変換します。

### 手順

1. ファイルをドラッグ&ドロップ、またはボタンで追加する。
2. 必要に応じて出力先フォルダを変更する（デフォルト: 最初のファイルと同じフォルダ）。
3. 必要に応じてExcelオプションを設定する。
4. 「🔄 PDF変換実行」ボタンをクリックする。
5. 変換完了後、出力フォルダが自動で開きます。

### Excelオプション

- **「Excelシートが複数ある場合は、すべてのシートをそれぞれPDFに変換する」**  
  オンにすると、1ファイルの各シートを個別のPDFとして出力します。  
  オフにすると、印刷設定に従い1つのPDFとして出力します。  
  ※ 印刷範囲が設定されていないシートは自動的にスキップされます。

### 出力

- 指定した出力先フォルダにPDFが作成されます（デフォルト: 元ファイルと同じフォルダ）。
- PDFファイルをドロップした場合は、変換不要のためそのまま出力先にコピーされます。

---

## 資料NO挿入タブ

PDFファイルのヘッダー右上に資料番号を挿入します。元ファイルは変更されません。

### 手順

1. PDFファイルをドラッグ&ドロップ、またはボタンで追加する。
2. 必要に応じて出力先フォルダを変更する。
3. 設定（文字・番号方式・フォント等）を行う。
4. 「🔍 プレビュー」ボタンで挿入結果を確認できます（オプション）。
5. 「📝 資料NO挿入実行」ボタンをクリックし、確認ダイアログで「はい」をクリックする。

### 設定項目

#### 文字選択

| 選択肢 | 動作 |
|--------|------|
| 資料 | ヘッダーに「資料」を挿入 |
| 参考 | ヘッダーに「参考」を挿入（番号なしで固定） |
| その他 | 任意のテキストを入力（10文字以内） |

#### 番号方式

| 方式 | 例 |
|------|-----|
| 連番 | 資料1, 資料2, 資料3 ... |
| ハイフン連番 | 資料1-1, 資料1-2, 資料1-3 ... |
| 固定番号 | 資料5（全ファイル同一番号） |
| 番号なし | 資料（番号なし、全ファイル共通） |

#### フォント

メイリオ・MSゴシック・MS明朝・游ゴシック・BIZ UDPゴシックから選択できます。

#### 挿入ページ

- **表紙（1ページ目）のみ**（デフォルト）: 各PDFの1ページ目だけに挿入
- **全ページ**: 全ページに挿入

#### ファイル名先頭に資料番号を追加する

オンにすると、出力ファイル名の先頭に「【資料１】」のような形式で資料番号が追加されます（全角数字）。

#### 左綴じ対応

オンにすると、A3縦・横長ページ（A4横・PowerPoint 16:9等）の資料番号を右下に配置します。

### 出力

- 指定した出力先フォルダに新規PDFが作成されます。
- **元ファイルは変更されません。**

---

## PDF結合タブ

複数のPDFファイルを1つに結合します。

### 手順

1. PDFファイルをドラッグ&ドロップ、またはボタンで追加する。
2. ファイルの順序を確認・調整する（ドラッグまたは↑↓ボタン）。
3. 必要に応じて出力先フォルダを変更する。
4. オプションを設定する。
5. 「📋 PDF結合実行」ボタンをクリックする。

### 出力

- 出力先フォルダに **`結合_YYYYMMDD_HHMMSS.pdf`** という名前で自動保存されます。
- 保存先を毎回指定する必要はありません。

### オプション

#### 奇数ページのPDF末尾に白紙ページを挿入する

両面印刷時に次のPDFが必ず右ページ（奇数ページ）から始まるよう、末尾ページ数が奇数のPDFに白紙ページを追加します。

#### フッター中央にページ番号を挿入する

結合後のPDFの各ページのフッター中央にページ番号を印刷します。

| 設定 | 説明 |
|------|------|
| 開始ページ | 何ページ目からページ番号を印刷するか |
| 開始番号 | ページ番号の開始値（例: 5 → 5, 6, 7 ...） |

#### 左綴じ対応（ページ番号）

オンにすると、A3縦・横長ページのページ番号位置を左綴じ製本向けに変更します。

---

## ページ番号挿入タブ

1つのPDFファイルにページ番号を挿入します。元ファイルは変更されません。

### 手順

1. PDFファイルをドラッグ&ドロップ、またはボタンで選択する。
2. 必要に応じて出力先フォルダを変更する。
3. 開始ページ・開始番号・フォントを設定する。
4. 「🔍 プレビュー」ボタンで確認できます（オプション）。
5. 「📄 ページ番号挿入実行」ボタンをクリックし、確認ダイアログで「はい」をクリックする。

### 設定項目

| 設定 | 説明 |
|------|------|
| 開始ページ | 何ページ目からページ番号を印刷するか |
| 開始番号 | ページ番号の開始値 |
| フォント | メイリオ・MSゴシック・MS明朝・游ゴシック・BIZ UDPゴシックから選択 |
| 左綴じ対応 | A3縦・横長ページのページ番号位置を変更 |

### 出力

- 指定した出力先フォルダに新規PDFが作成されます。
- **元ファイルは変更されません。**

---

## アップデート通知

新しいバージョンが公開されると、ウィンドウ上部に黄色いバナーで通知されます。「GitHubで確認」ボタンをクリックするとリリースページが開きます。✕ ボタンで閉じることができます。

---

## よくある質問

**Q. Word/Excel/PowerPointの変換に失敗する**  
A. 対象のアプリケーション（Word, Excel, PowerPoint）がインストールされている必要があります。  
&emsp;また、変換中に対象ファイルを開いたまま操作しないでください。

**Q. OneDrive・SharePoint上のファイルで変換に失敗する**  
A. ファイルのロック競合を自動回避する処理が組み込まれています。それでも失敗する場合は、ファイルをローカルフォルダにコピーしてから変換してください。

**Q. Excelの一部のシートがPDFに含まれない**  
A. 印刷範囲が設定されていないシートや、非表示シートはスキップされます。これは正常な動作です。

**Q. 変換済みファイルが見つからない**  
A. 出力先フォルダを確認してください（デフォルトは元ファイルと同じフォルダ）。変換完了後に出力フォルダが自動で開きます。

**Q. 同じファイルを2回処理したら「(2)」が付いたファイルができた**  
A. 同名ファイルが既に存在するとき、上書きを避けるために自動的に連番を付与します。これは正常な動作です（Windowsのコピー操作と同じ）。

**Q. ドラッグ&ドロップが反応しない**  
A. ウィンドウ上の空白部分にドロップしてください。

---

## バージョン履歴

| バージョン | 内容 |
|------------|------|
| 1.20.0 | 出力先統一・非破壊的出力への変更。全タブに出力先フォルダ指定UI追加。結合タブの保存先ダイアログ廃止（自動ファイル名）。元ファイルバックアップフォルダ廃止 |
| 1.19.0 | MANUAL.md を最新版に全面更新 |
| 1.16.1 | 資料NO挿入タブの設定順を変更（文字選択→番号設定→フォント） |
| 1.16.0 | 資料NO挿入・ページ番号挿入タブにフォント選択プルダウン追加 |
| 1.15.0 | OneDrive/SharePoint クラウドパス検出を強化（レジストリ読み取りによる組織名不問の自動検出） |
| 1.14.0 | Excel全シートPDF変換の空ページエラー修正 |
| 1.13.0 | 変換後・資料NO挿入後の自動タブ切替ダイアログを廃止 |
| 1.12.0 | 資料NO挿入に左綴じ対応・全ページ挿入・プレビュー機能を追加 |
| 1.11.1 | インストーラー形式での配布対応、アップデート通知機能追加 |
| 1.0.0 | 初回リリース |

---

GitHub: https://github.com/mozu93/PDFchangecombine
