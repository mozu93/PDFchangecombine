# PDF変換・結合ツール ユーザーマニュアル

バージョン 1.21.8

---

## 概要

PDF変換・結合ツールは、Office文書・画像ファイルのPDF変換、複数PDFの結合、資料番号挿入、ページ番号挿入をワンストップで行えるツールです。

**すべての処理において元ファイルは変更されず、そのままの場所に残ります。** 出力は指定フォルダに新規ファイルとして作成されます。

### 対応ファイル形式

| 種類 | 拡張子 |
|------|--------|
| Word | .docx, .doc |
| Excel | .xlsx, .xls |
| PowerPoint | .pptx, .ppt |
| 画像 | .jpg, .jpeg, .png, .bmp, .gif, .tiff |
| PDF | .pdf |

---

## 共通操作

### ファイルの追加方法

- **ドラッグ&ドロップ**: ファイルやフォルダをウィンドウ上にドロップ。フォルダをドロップすると内部のファイルを自動検索します。
- **ボタン**: 「ファイル選択」「PDF選択」ボタンをクリックしてダイアログから選択。

### ファイルの並び替え

- リスト内のファイルをドラッグして順序を変更できます（資料NO挿入・PDF結合タブ）。
- ↑ / ↓ ボタンでも順序を変更できます。
- **Ａ↓** ボタンでファイル名の昇順に自動ソートできます。

### 出力先フォルダの指定（全タブ共通）

各タブに **「出力先: …」** の行があります。

| 操作 | 動作 |
|------|------|
| 何も指定しない（既定） | リスト先頭（最初）のファイルがあるフォルダの中に、機能別のフォルダを自動作成して保存する |
| **📂 変更** ボタン | フォルダ選択ダイアログで任意のフォルダに変更できる。指定した場合はそのフォルダ直下に保存され（サブフォルダは作成されない）、指定はアプリを終了するまで有効（次回起動時は既定に戻る） |

既定の出力先フォルダ名はタブごとに異なります。

| タブ | 既定の出力先フォルダ名 |
|------|------------------------|
| PDF変換 | PDF変換済 |
| 資料NO挿入 | 資料NO挿入済 |
| PDF結合 | PDF結合済 |
| ページ番号挿入 | ページ番号挿入済 |

**出力先は記憶されません。** 📂変更で指定したフォルダはアプリ終了までのみ有効で、次回起動時は上記の既定フォルダに戻ります。

**同名ファイルが既に存在する場合**、上書きしてよいか確認ダイアログが表示されます。「はい」なら同じ名前で上書き、「いいえ」なら処理を中止します（元ファイル自体は変換・結合等の対象であっても上書きされません）。

### 元ファイルの扱い（全タブ共通）

元ファイルは移動・変更されず、そのままの場所に残ります。

---

## PDF変換タブ

Office文書・画像ファイルをPDFに変換します。

### 手順

1. ファイルをドラッグ&ドロップ、またはボタンで追加する。
2. 必要に応じて出力先フォルダを変更する（デフォルト: 最初のファイルと同じフォルダ内の「PDF変換済」フォルダ）。
3. 必要に応じてExcelオプションを設定する。
4. 「🔄 PDF変換実行」ボタンをクリックする。
5. 変換完了後、出力フォルダが自動で開きます。

### Excelオプション

- **「Excelシートが複数ある場合は、すべてのシートをそれぞれPDFに変換する」**  
  オンにすると、1ファイルの各シートを個別のPDFとして出力します。  
  オフにすると、印刷設定に従い1つのPDFとして出力します。  
  ※ 印刷範囲が設定されていないシートは自動的にスキップされます。

### 出力

- 指定した出力先フォルダにPDFが作成されます（デフォルト: 最初のファイルと同じフォルダ内の「PDF変換済」フォルダ）。
- PDFファイルをドロップした場合は、変換不要のためそのまま出力先にコピーされます。

---

## 資料NO挿入タブ

PDFファイルのヘッダー右上に資料番号を挿入します。元ファイルは変更されません。

### 手順

1. PDFファイルをドラッグ&ドロップ、またはボタンで追加する。
2. 必要に応じて出力先フォルダを変更する（デフォルト: 最初のファイルと同じフォルダ内の「資料NO挿入済」フォルダ）。
3. 文字・番号方式を設定する。フォント・左綴じ対応・全ページ挿入・白背景などは「▼ 詳細設定」を開いて設定する。
4. 「🔍 プレビュー」ボタンで挿入結果を確認できます（オプション）。
5. 「📝 資料NO挿入実行」ボタンをクリックし、確認ダイアログ（出力先が表示されます）で「はい」をクリックする。

### 設定項目

#### 文字選択

| 選択肢 | 動作 |
|--------|------|
| 資料 | ヘッダーに「資料」を挿入 |
| 参考 | ヘッダーに「参考」を挿入（番号なしで固定） |
| その他 | 任意のテキストを入力（10文字以内） |

#### 番号方式

| 方式 | 例 |
|------|-----|
| 連番 | 資料1, 資料2, 資料3 ... |
| ハイフン連番 | 資料1-1, 資料1-2, 資料1-3 ... |
| 固定番号 | 資料5（全ファイル同一番号） |
| 番号なし | 資料（番号なし、全ファイル共通） |

#### フォント

メイリオ・MSゴシック・MS明朝・游ゴシック・BIZ UDPゴシックから選択できます。

#### 挿入ページ

- **表紙（1ページ目）のみ**（デフォルト）: 各PDFの1ページ目だけに挿入
- **全ページ**: 全ページに挿入

#### ファイル名先頭に資料番号を追加する

オンにすると、出力ファイル名の先頭に「【資料１】」のような形式で資料番号が追加されます（全角数字）。

#### 左綴じ対応

オンにすると、A3縦・横長ページ（A4横・PowerPoint 16:9等）の資料番号を右下に配置します。

#### 資料番号の枠内を白色にする

オンにすると、資料番号の四角い枠内を白色で塗り、元のPDF内容が文字と重なるのを防ぎます。オフの場合は従来どおり透明です。

### 出力

- 指定した出力先フォルダに新規PDFが作成されます（デフォルト: 最初のファイルと同じフォルダ内の「資料NO挿入済」フォルダ）。
- **元ファイルは変更されません。**

---

## PDF結合タブ

複数のPDFファイルを1つに結合します。

### 手順

1. PDFファイルをドラッグ&ドロップ、またはボタンで追加する。
2. ファイルの順序を確認・調整する（ドラッグまたは↑↓ボタン）。
3. 必要に応じて出力先フォルダを変更する（デフォルト: 最初のファイルと同じフォルダ内の「PDF結合済」フォルダ）。
4. 白紙挿入・ページ番号・左綴じ対応などのオプションは「▼ 詳細設定」を開いて設定する。
5. 「📋 PDF結合実行」ボタンをクリックする。

### 出力

- 出力先フォルダに **`【結合】（先頭ファイル名）.pdf`** という名前で自動保存されます。
- 同名ファイルが既に存在する場合は、上書きするかどうかの確認ダイアログが表示されます。「いいえ」を選ぶと `(2).pdf` のように連番付きの別ファイルとして保存されます。

### 資料を差し替え...（資料の差し替え・追加・削除・一括リナンバリング）

本アプリで結合したPDF（資料NOの構成情報を内部に持つもの）を選び、資料単位での変更が行えます。

1. ツールバーの「資料を差し替え...」ボタンから、対象の結合済みPDFを選択する（ドラッグ&ドロップも可）。
2. 資料ごとに「差し替え」「＋この後に追加」「削除」で変更したい内容を指定する。
   - この時点ではまだファイルは作られない（一覧に「差し替え予定」「追加予定」「削除予定」として積み上がる）。
   - 「取り消す」で個別に、または「変更をすべて取り消す」でまとめて撤回できる。
3. 変更をすべて指定したら「変更を実行」をクリックする。内容の確認後、**まとめて1回だけ**適用され、出力ファイルが1つだけ作られる（中間ファイルは作られない）。
4. 資料番号を連番に振り直したい場合は「🔢 一括リナンバリング...」を使う（積んだ変更が無い状態でのみ実行可能）。

いずれも元の結合済みPDFは変更されず、新しいファイルとして出力されます。

### オプション

#### 奇数ページのPDF末尾に白紙ページを挿入する

両面印刷時に次のPDFが必ず右ページ（奇数ページ）から始まるよう、末尾ページ数が奇数のPDFに白紙ページを追加します。

#### フッター中央にページ番号を挿入する

結合後のPDFの各ページのフッター中央にページ番号を印刷します。

| 設定 | 説明 |
|------|------|
| 開始ページ | 何ページ目からページ番号を印刷するか |
| 開始番号 | ページ番号の開始値（例: 5 → 5, 6, 7 ...） |

#### 左綴じ対応（ページ番号）

オンにすると、A3縦・横長ページのページ番号位置を左綴じ製本向けに変更します。

---

## ページ番号挿入タブ

1つのPDFファイルにページ番号を挿入します。元ファイルは変更されません。

### 手順

1. PDFファイルをドラッグ&ドロップ、またはボタンで選択する。
2. 必要に応じて出力先フォルダを変更する（デフォルト: 最初のファイルと同じフォルダ内の「ページ番号挿入済」フォルダ）。
3. 開始ページ・開始番号・フォントを設定する。
4. 「🔍 プレビュー」ボタンで確認できます（オプション）。
5. 「📄 ページ番号挿入実行」ボタンをクリックし、確認ダイアログ（出力先が表示されます）で「はい」をクリックする。

### 設定項目

| 設定 | 説明 |
|------|------|
| 開始ページ | 何ページ目からページ番号を印刷するか |
| 開始番号 | ページ番号の開始値 |
| フォント | メイリオ・MSゴシック・MS明朝・游ゴシック・BIZ UDPゴシックから選択 |
| 左綴じ対応 | A3縦・横長ページのページ番号位置を変更 |

### 出力

- 指定した出力先フォルダに新規PDFが作成されます（デフォルト: 最初のファイルと同じフォルダ内の「ページ番号挿入済」フォルダ）。
- **元ファイルは変更されません。**

---

## アップデート通知

新しいバージョンが公開されると、ウィンドウ上部に黄色いバナーで通知されます。「GitHubで確認」ボタンをクリックするとリリースページが開きます。✕ ボタンで閉じることができます。

また、アプリを更新した後に起動すると、前回起動時と違うバージョンであれば「更新内容」ポップアップが自動表示され、そのバージョンで何が変わったかを確認できます（初回インストール時は表示されません）。

---

## よくある質問

**Q. Word/Excel/PowerPointの変換に失敗する**  
A. 対象のアプリケーション（Word, Excel, PowerPoint）がインストールされている必要があります。  
&emsp;また、変換中に対象ファイルを開いたまま操作しないでください。

**Q. OneDrive・SharePoint上のファイルで変換に失敗する**  
A. ファイルのロック競合を自動回避する処理が組み込まれています。それでも失敗する場合は、ファイルをローカルフォルダにコピーしてから変換してください。

**Q. Excelの一部のシートがPDFに含まれない**  
A. 印刷範囲が設定されていないシートや、非表示シートはスキップされます。これは正常な動作です。

**Q. 変換済みファイルが見つからない**  
A. 出力先フォルダを確認してください（デフォルトは最初のファイルと同じフォルダ内の機能別フォルダ、例: 「PDF変換済」）。変換完了後に出力フォルダが自動で開きます。

**Q. 同じファイルを2回処理したら上書き確認が出た／「(2)」が付いたファイルができた**  
A. 同名ファイルが既に存在するときは上書き確認ダイアログが表示されます。「はい」で上書き、「いいえ」を選ぶと `(2).pdf` のように連番付きの別ファイルになります。

**Q. ドラッグ&ドロップが反応しない**  
A. ウィンドウ上の空白部分にドロップしてください。

---

## バージョン履歴

| バージョン | 内容 |
|------------|------|
| 1.21.8 | バージョンアップ時のリリースノートポップアップが配布用実行ファイルで表示されない不具合を修正（ビルド設定への同梱漏れ）。GitHubリリースページの「変更内容」に実際のリリースノートが表示されるようビルド手順を修正 |
| 1.21.7 | 「資料を差し替え」機能（PDF結合タブから、資料単位での差し替え・追加・削除・一括リナンバリングが可能）を追加。複数の変更をまとめて1回で実行できるように変更し、出力ファイルが都度増える問題を解消。差し替えダイアログにドラッグ&ドロップ対応を追加。結合済みPDFに後からページ番号を付けても資料構成が保持されるよう修正。変換・資料NO挿入・結合・ページ番号の全機能で、同名ファイルの上書き確認に対応。バージョンアップ時にリリースノートをポップアップ表示。ファイルリストの単一選択・ツールバー幅など細かいUI修正 |
| 1.21.6 | 資料NO挿入・PDF結合タブの詳細設定を折りたたみ式に変更し画面をすっきり表示。タブ表示をアイコン+短縮ラベルに変更（正式名称はホバーで表示）。ドラッグ&ドロップ時の挿入位置表示とドラッグ中のファイルの見た目を改善 |
| 1.21.5 | Excel変換でA4 PDFのページ寸法が約4/3倍になった場合、A4印刷時に資料NOが縮小されないよう文字・余白・枠・白背景を自動補正 |
| 1.21.4 | 資料NOの四角い枠内を白色にできるオプションを追加。透明／白色を選択でき、設定保存とプレビューに対応 |
| 1.21.3 | 出力先を記憶せず機能別既定フォルダ（PDF変換済・資料NO挿入済・PDF結合済・ページ番号挿入済）へ保存する仕様に変更。結合ファイル名を「【結合】先頭ファイル名.pdf」に変更。元ファイルの「変換元」フォルダ退避を廃止（元の場所に残置） |
| 1.21.2 | 資料NO挿入時、90度・180度回転ページで資料NOが誤った位置・向きに挿入される不具合を修正 |
| 1.21.1 | 資料NOフォントサイズに14pt・12ptを追加。①などの機種依存文字はBIZ UDPゴシック選択時のみ入力エラーとして拒否するよう修正 |
| 1.21.0 | 処理完了後、元ファイルを「変換元」フォルダへ自動移動する機能を全タブに追加 |
| 1.20.1 | ページ番号挿入時、90度回転ページで数字が上下逆さまに表示される不具合を修正 |
| 1.20.0 | 出力先統一・非破壊的出力への変更。全タブに出力先フォルダ指定UI追加。結合タブの保存先ダイアログ廃止（自動ファイル名）。元ファイルバックアップフォルダ廃止 |
| 1.19.0 | MANUAL.md を最新版に全面更新 |
| 1.16.1 | 資料NO挿入タブの設定順を変更（文字選択→番号設定→フォント） |
| 1.16.0 | 資料NO挿入・ページ番号挿入タブにフォント選択プルダウン追加 |
| 1.15.0 | OneDrive/SharePoint クラウドパス検出を強化（レジストリ読み取りによる組織名不問の自動検出） |
| 1.14.0 | Excel全シートPDF変換の空ページエラー修正 |
| 1.13.0 | 変換後・資料NO挿入後の自動タブ切替ダイアログを廃止 |
| 1.12.0 | 資料NO挿入に左綴じ対応・全ページ挿入・プレビュー機能を追加 |
| 1.11.1 | インストーラー形式での配布対応、アップデート通知機能追加 |
| 1.0.0 | 初回リリース |

---

GitHub: https://github.com/mozu93/PDFchangecombine
