# PDF変換・結合ツール ユーザーマニュアル

バージョン 1.11.1

---

## 概要

PDF変換・結合ツールは、Office文書・画像ファイルのPDF変換、複数PDFの結合、PDFへの資料番号挿入をワンストップで行えるツールです。

### 対応ファイル形式

| 種類 | 拡張子 |
|------|--------|
| Word | .docx, .doc |
| Excel | .xlsx, .xls |
| PowerPoint | .pptx, .ppt |
| 画像 | .jpg, .jpeg, .png, .bmp, .gif, .tiff |
| PDF | .pdf |

---

## 共通操作

### ファイルの追加方法

ファイルは以下の2通りの方法で追加できます。

- **ドラッグ&ドロップ**: ファイルやフォルダをウィンドウ上にドロップ。フォルダをドロップすると内部のファイルを自動検索します。
- **ボタン**: 「ファイル追加」「PDF追加」「PDFファイル選択」ボタンをクリックしてダイアログから選択。

### ファイルの並び替え

- リスト内のファイルをドラッグして順序を変更できます（資料NO挿入・PDF結合タブ）。
- ↑ / ↓ ボタンでも順序を変更できます。

---

## PDF変換タブ

Office文書・画像ファイルをPDFに変換します。

### 手順

1. ファイルをドラッグ&ドロップ、またはボタンで追加する。
2. 必要に応じてExcelオプションを設定する。
3. 「PDF変換開始」ボタンをクリックする。
4. 変換完了後、出力フォルダが自動で開きます。

### Excelオプション

- **「Excelシートが複数ある場合は、すべてのシートをそれぞれPDFに変換する」**  
  オンにすると、1ファイルの各シートを個別のPDFとして出力します。  
  オフにすると、印刷設定に従い1つのPDFとして出力します。

### 出力先

元ファイルと同じフォルダ内に「変換済」フォルダが作成され、変換後のPDFが保存されます。

### 変換後の流れ

変換完了時に「資料NO挿入」への移行を提案するダイアログが表示されます。

---

## 資料NO挿入タブ

PDFファイルのヘッダー右上に資料番号を挿入します。

### 手順

1. PDFファイルをドラッグ&ドロップ、またはボタンで追加する。
2. 設定を行う。
3. 「資料NO挿入実行」ボタンをクリックする。
4. 確認ダイアログで「はい」をクリックすると処理が開始されます。

### 設定項目

#### 文字選択

| 選択肢 | 動作 |
|--------|------|
| 資料 | ヘッダーに「資料」を挿入（デフォルト） |
| 参考 | ヘッダーに「参考」を挿入 |

#### 番号方式

| 方式 | 例 |
|------|-----|
| 連番 | 資料1, 資料2, 資料3 ... |
| ハイフン連番 | 資料1-1, 資料1-2, 資料1-3 ... |
| 固定番号 | 資料5（全ファイル同一番号） |
| 番号なし | 資料（番号なし、全ファイル共通） |

#### 開始番号

連番・ハイフン連番モードで使用。何番から始めるかを指定します。

#### ファイル名先頭に資料番号を追加する

オンにすると、ファイル名の先頭に「【資料１】」のような形式で資料番号が追加されます（全角数字）。

### 出力・バックアップ

- 元ファイルは自動的に「**元ファイル**」フォルダにバックアップされます。
- 挿入済みのファイルは元のファイル名で同じ場所に保存されます。

### 挿入後の流れ

複数ファイルを処理した場合、「PDF結合」への移行を提案するダイアログが表示されます。

---

## PDF結合タブ

複数のPDFファイルを1つに結合します。

### 手順

1. PDFファイルをドラッグ&ドロップ、またはボタンで追加する。
2. ファイルの順序を確認・調整する（ドラッグまたは↑↓ボタン）。
3. オプションを設定する。
4. 「PDF結合実行」ボタンをクリックする。
5. 保存先のファイル名と場所を指定する。

### オプション

#### 奇数ページのPDF末尾に白紙ページを挿入する

両面印刷時に次のPDFが必ず右ページ（奇数ページ）から始まるよう、末尾ページ数が奇数のPDFに白紙ページを追加します。

#### フッター中央にページ番号を挿入する

結合後のPDFの各ページのフッター中央にページ番号を印刷します。

| 設定 | 説明 |
|------|------|
| 開始ページ | 何ページ目からページ番号を印刷するか |
| 開始番号 | ページ番号の開始値（例: 5 → 5, 6, 7 ...） |

---

## アップデート通知

新しいバージョンが公開されると、ウィンドウ上部に黄色いバナーで通知されます。「GitHubで確認」ボタンをクリックするとリリースページが開きます。✕ ボタンで閉じることができます。

---

## よくある質問

**Q. Word/Excel/PowerPointの変換に失敗する**  
A. 対象のアプリケーション（Word, Excel, PowerPoint）がインストールされている必要があります。  
&emsp;また、変換中に対象ファイルを開いたまま操作しないでください。

**Q. ドラッグ&ドロップが反応しない**  
A. ウィンドウ上の空白部分にドロップしてください。リスト内のファイル行はドラッグ&ドロップ対象外の場合があります。

**Q. 変換済みファイルが見つからない**  
A. 元ファイルと同じフォルダ内の「変換済」フォルダを確認してください。  
&emsp;変換完了後に出力フォルダが自動で開きます。

**Q. 資料NO挿入で元ファイルを復元したい**  
A. 元ファイルと同じフォルダ内の「元ファイル」フォルダにバックアップが保存されています。

---

## バージョン履歴

| バージョン | 内容 |
|------------|------|
| 1.11.1 | インストーラー形式での配布対応、アップデート通知機能追加 |
| 1.11.0 | D&Dバグ修正、UIオプション改善 |
| 1.0.0 | 初回リリース |

---

GitHub: https://github.com/mozu93/PDFchangecombine
